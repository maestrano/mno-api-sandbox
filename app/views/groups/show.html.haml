.row
  .span8.offset2
    .well
      %p
        %b Name:
        = @group.name
        %br
        %b Universal ID:
        = @group.uid
        %br
        %b Use Application:
        = @group.app.name

      = link_to 'Edit', edit_group_path(@group)
      \|
      = link_to 'Back', groups_path
